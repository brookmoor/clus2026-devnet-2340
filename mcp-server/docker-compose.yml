version: '3.8'

services:
  ctb-mcp:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - CTB_MANAGER_IP=${CTB_MANAGER_IP}
      - CTB_USERNAME=${CTB_USERNAME}
      - CTB_PASSWORD=${CTB_PASSWORD}
